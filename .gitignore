.agentkore/
.opencode/
.web-cache/
*.cache.db

# Personal/local artifacts — PII protection
cleanup_repos.sh
patches/

# Secrets and credentials
.env
.env.local
.env.*
*.env
!.env.example
*.pem
*.key
**/creds/**
**/credentials*
inbox.conf

# Hermes state databases (session data, not config)
.hermes/
state.db
*.db
*.db-journal
*.db-wal
*.db-shm

# Memory files (gitignored per brain source — root level too for safety)
MEMORY.md
USER.md

# Logs
logs/
*.log
*.pid

# Auth secrets
.htpasswd

# Secrets and credentials (extended)
*secret*
*credential*
*cred*
*token*
*.p12
.netrc

# OS junk
.DS_Store
Thumbs.db

# Offline ZIM content (large downloads — don't commit)
offline/zim/
*.zim

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Git filter-repo artifacts
# Agent infrastructure
.hermes-cortex/memory/
.hermes-cortex/sessions/archive/
.hermes-cortex/sessions/current.md
IMPROVEMENTS.md
agent-inbox-private/

# Agent registry (PII — real hostnames, URLs, IPs)
src/agent-registry.json
# Use src/agent-registry.json.example as the template
.governance-*.json
deploy/
src/scripts/
plugins/
src/scripts/install-crons.sh
