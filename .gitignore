.agentkore/
.opencode/
.web-cache/
*.cache.db

# Personal/local artifacts — PII protection
cleanup_repos.sh
patches/
.hermes-cortex/plugins/

# Secrets and credentials
.env
.env.local
.env.*
*.env
!.env.example
*.pem
*.key
**/creds/**
**/credentials*
inbox.conf

# Hermes state databases (session data, not config)
.hermes/
state.db
*.db
*.db-journal
*.db-wal
*.db-shm

# Memory files (gitignored per brain source — root level too for safety)
MEMORY.md
USER.md

# Logs
logs/
*.log
*.pid

# Auth secrets
.htpasswd

# Secrets and credentials (extended)
*secret*
*credential*
*cred*
*token*
*.p12
# Negate our own scripts that legitimately contain "secret"
!ops/scripts/secret-leak-detector.sh
!ops/scripts/secret-leak-watchdog.py
# Negate our own skills that legitimately contain "credential" or "secret"
!**/secure-credential-handling/**
!**/credential-leak-response/
!**/credential-leak-response/**
!ops/scripts/agent/agent-worker.py
!ops/scripts/agent/install-worker.sh
!ops/scripts/orch-bus/orch-bus-depth-watchdog.sh
!ops/scripts/orch-bus/orch-bus-audit-watchdog.py
!ops/scripts/orch-bus/orch-bus-forwarder.py
!ops/scripts/orch-bus/orch-bus-confirmation-poller.py
!ops/scripts/orch-bus/orch-bus-confirmation-alert.sh
!ops/scripts/orch-bus/orch-bus-recover-timeouts.sh
!ops/scripts/orch-bus/orch-bus-workflow-inspector.py
!ops/scripts/orch-bus/orch-bus-sensor.py

# OS junk
.DS_Store
Thumbs.db

# Offline ZIM content (large downloads — don't commit)
offline/zim/
*.zim

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Git filter-repo artifacts
# Agent infrastructure
.hermes-cortex/memory/
.hermes-cortex/sessions/archive/
.hermes-cortex/sessions/current.md
IMPROVEMENTS.md

# Agent registry (PII — real hostnames, URLs, IPs)
agent-registry.json
# Use ops/install/deploy/agent-registry.json.example as the template
.governance-*.json
/deploy/
# Per-machine allow list — NEVER commit (PII: real client IPs live only
# at /etc/nginx/allow-ips-manual.conf on each host)
allow-ips-manual.conf
# ops/scripts/ is fully tracked — every script in here is intentional
# and worth sharing with the fleet. New scripts are auto-tracked.
# (Previously used per-file whitelist; fell out of sync too often.)
/runtime
# Governance lock marker — written by begin_change() inside repo
# as secondary protection alongside the primary lock in ~/.hermes-cortex/state/
.hermes-cortex/.governance-lock
private-data/
# mycortex DB backups (e.g. pre-migration dumps) — large, PII-heavy, never commit
ops/services/mycortex/backups/
