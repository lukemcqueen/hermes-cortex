---
description: Debug failing tests, runtime errors, tools, or production issues
---

Load `debugging`.

If production/customer impact exists, also load `incident-debug`.
If proxy/tool-call related, load `proxy-enforcer`, `proxy-tool-calling`, and `local-llm-reliability`.

Use evidence → isolate → hypothesis → test → fix → verify.
