---
description: Execute exactly one task using small verified changes
---

Load `agent-contract`, `agent-flow`, `task-executor`, `change-test-loop`, and relevant stack skill.

Execute one task only. Start with inspect → plan → change → verify → report. Use change-test-loop for the verify loop. Report format from agent-contract.
