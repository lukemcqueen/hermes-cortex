---
description: Plan work using AgentKore — choose the right planning depth
---

Load skills as needed per phase below.

## Phase selection

| Task clarity | Path |
|---|---|
| Clear/small | Load `fast-bmad` → produce intent, value, constraints, smallest slice, files, verification |
| Fuzzy requirements | Load `ak-elicit` → goal, stakeholders, value, constraints, must-haves, risks, open questions, acceptance criteria, first slice |
| Feature spec | Load `prd-lite` → create/update PRD. Then `story-slicing` → `prd-to-tasks` for ordered testable slices fitting one change-test-loop each |
| Enterprise architecture | Load `ak-party` → review product, architecture, security, data, DevOps, QA perspectives; surface tradeoffs, risks, verification strategy, first slices |

Output assumptions, chosen path, first slice, and verification.
