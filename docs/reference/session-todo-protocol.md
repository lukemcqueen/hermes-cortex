## Session Todo Protocol


Every session follows this discipline:

1. **On session start** — read `~/.hermes-cortex/data/TODO.md` (durable cross-session file). Then `todo()` to mirror in the session tool. Then `session_search(

> Moved from AGENTS.md by `agents-doc-audit.py --prune --apply`
> Date: 2026-07-21T19:31:27.193654+00:00
