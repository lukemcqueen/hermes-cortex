# hc.env — Hermes Cortex CLI config
#
# Copy this to ~/.hermes-cortex/hc.env and fill in your value.
# hc runs direct against Postgres (docker exec) — no auth needed for admin ops.
#
# =============================================================================
# OPTIONAL — Default agent name (default: moses)
# =============================================================================
HC_AGENT=moses
