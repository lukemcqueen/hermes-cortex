# Session 2026-07-23 — Proactive Scan Sequence

Exact commands and outputs from the first proactive scan execution. Use as a verification reference.

## Initial State
```
Doctor: FAILING (40 pass · 1 warn · 1 fail · 1 info)
Issues: ❌ AGENTS.md (hermes-cortex), ⚠️ Symlinks, ℹ️ Extra crons
```

## Fix 1: Stale Governance Symlink
```bash
# Identified by doctor: ⚠️ Symlinks
# Broken: .governance-generic.json → stale session lock
rm ~/hermes-cortex/.governance-generic.json

# Verify
# The .governance-sess_<id>.json lock was from a completed session.
```

## Fix 2: Stale AGENTS.md
```bash
# Identified by doctor: ❌ AGENTS.md (hermes-cortex)
# Check what changed recently:
cd ~/hermes-cortex && git diff HEAD~10..HEAD --name-only

# Updates made:
# - Key Directories table: added agent-architecture, fleet-update-protocol,
#   docs/prd/, agent profiles, agent-registry
# - Reference Docs table: added 3 new entries
# - Footer date updated

# Sync deployed copy:
cp ~/hermes-cortex/AGENTS.md ~/.hermes/AGENTS.md

# Commit and push:
cd ~/hermes-cortex && git add AGENTS.md
git commit -m "Update AGENTS.md: add references to agent-architecture, fleet-update-protocol, PRDs, agent profiles"
git push origin main
```

## Final State
```
Doctor: HEALTHY (41 pass · 0 warn · 0 fail · 1 info)
```
