---
name: reasoning-patterns
version: 1.1.0
category: software-development
description: "DEPRECATED alias — merged into agent-flow (2026-08-20). Load agent-flow instead; its embedded reasoning-pattern table covers Plan-Execute-Verify, ReAct, Reflexion, and Tree of Thoughts."
pinned: true
---

# DEPRECATED — use `agent-flow`

This skill was folded into `agent-flow` on 2026-08-20 (its frontmatter carries `aliases: [reasoning-patterns]`). The pattern table below lives there.

## What to do instead

`skill_view('agent-flow')` → "How to route" → step 1 (match the reasoning pattern). Same four patterns: Plan-Execute-Verify (default), ReAct (debug/exploration), Reflexion (quality-critical), Tree of Thoughts (design trade-offs). State your choice explicitly.
